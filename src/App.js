import EmailTagger from "./modules/email-tagger/views";

function App() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        marginTop: "50px",
      }}
    >
      <EmailTagger />
    </div>
  );
}

export default App;
